//go:build (dax || config || all) && !ignore_autogenerated

package dax

import (
	"github.com/crossplane/upjet/pkg/config"

	pconfig "github.com/upbound/provider-aws/config"
)

// Configure adds configurations for the dax group.
func Configure(p *config.Provider) {
	p.AddResourceConfigurator("aws_dax_cluster", func(r *config.Resource) {
		r.UseAsync = true
	})
}

func init() {
	pconfig.ProviderConfiguration.AddConfig(Configure)
}
